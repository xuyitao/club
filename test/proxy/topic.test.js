var topic = require('../../proxy/topic');
var should = require('should');


describe('test/proxy/topic.test.js', function () {
	it('topic newAndSave', function (done) {

		// let title = 'title'
		// let content = 'content'
		// let tab = 'share'
		// topic.newAndSave(title, content, tab).then(function (item) {
		// 	should.exist(item)
		// 	done()
		// })
		done()
	})


})
